import {  Grid ,Scene } from '../index';

describe('add', () => {
    var cl = new  Scene();
    var cl2 = new Grid();
    it('should sum given numbers', () => {
        expect(cl.camera).toBe(undefined);
    });

});